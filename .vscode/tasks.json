{
    "version":"2.0.0",
    "tasks": [
        {
            "label": "g++ via pmt",
            "command": "./pmt", // Could be any other shell command
            "args": ["-e", "pmt.json"],
            "type": "shell"
        }
    ]
}